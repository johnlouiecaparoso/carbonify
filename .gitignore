# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Supabase CLI local link state (per-developer)
supabase/.temp/linked-project.json

.vercel
.env*

# Claude Code: share the project orientation doc, keep per-developer settings out.
# settings.local.json is Claude Code's convention for machine-local overrides
# (personal permission grants), so it must not be committed.
.claude/settings.local.json
